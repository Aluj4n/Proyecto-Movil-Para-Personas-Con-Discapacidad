<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/inicio"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/focus_background"
    tools:context=".Inicio">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/menuOpciones"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/focus_primary"
        app:titleTextColor="#FFFFFF"
        app:title="FocusFlow"
        app:titleTextAppearance="@style/TextAppearance.MaterialComponents.Headline6"
        app:navigationIcon="@null"
        app:subtitleTextColor="#FFFFFF"
        app:subtitle="¡Hola! Enfócate hoy 🧠"/>

    <FrameLayout
        android:id="@+id/contenedor"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@color/focus_background"/>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/focus_surface"
        app:itemIconTint="@color/focus_primary"
        app:itemTextColor="@color/focus_primary"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/menu_bottom"
        app:elevation="8dp"/>

</LinearLayout>